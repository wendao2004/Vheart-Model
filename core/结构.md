core/
├── bean/
│   ├── user.ts          ← User / LoginRequestDTO / LoginResponseDTO
│   ├── common.ts        ← CloudResult / SUCCESS_CODE
│   └── index.ts         ← 聚合导出
├── net/
│   ├── http.ts          ← callCloud 基础封装
│   ├── auth-api.ts      ← 登录/注册端点
│   └── index.ts         ← 聚合导出
├── data/
│   ├── mapper/
│   │   └── user-mapper.ts  ← DTO→Entity 转换（开闭原则核心）
│   ├── cache-keys.ts    ← 缓存 key 集中管理
│   └── user-data.ts     ← 登录/登出/本地存储
└── model/
    └── use-user-model.ts ← 业务编排 + 响应式状态
	
