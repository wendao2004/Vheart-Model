<template>
	<view class="navigation">
		<view class="topView">
			<text style="color: white;font-size: 60rpx;font-weight: 800;">我的</text>
		</view>
		<view class="userMessage">
			<view class="userImageBox">
				<image src="/static/logo.png" mode="aspectFill" class="userImage"></image>
			</view>
			<view class="userName">
				<text style="color: white;font-size: 30rpx;font-weight: 800;">{{userName}}</text>
				<text style="color: white;font-size: 30rpx;font-weight: 300;">{{userName}}</text>
			</view>
		</view>
		<view class="userMessageList">
			<view class="userMessageNumber">
				<text style="color: white;font-size: 30rpx;font-weight: 800;">12</text>
				<text style="color: white;font-size: 30rpx;font-weight: 800;">8</text>
				<text style="color: white;font-size: 30rpx;font-weight: 800;">2.3GB</text>
			</view>
			<view class="userMessageNumberName">
				<text style="color: white;font-size: 30rpx;font-weight: 800;">我的物品</text>
				<text style="color: white;font-size: 30rpx;font-weight: 800;">我的模型</text>
				<text style="color: white;font-size: 30rpx;font-weight: 800;">存储空间</text>
			</view>
		</view>
		<view class="toolsList">
			<view v-for="(n,title) in toolsList" :key="n.n" class="list">
				<image style="width: 72rpx;height: 72rpx;padding: 4%;" :src="n.url" mode=""></image>
				<text style="color: white;font-size: 30rpx;font-weight: 800;">{{n.title}}</text>
				<image style="width: 72rpx;height: 72rpx;padding: 4%;position: absolute;right: 0;"
					src="/static/logo.png" mode=""></image>
			</view>

		</view>
	</view>
</template>

<script setup>
	const userName = '用户123456'
	const toolsList = ref([
		{ n: 1, title: '我的收藏', url: '/static/logo.png' },
		{ n: 1, title: '浏览记录', url: '/static/logo.png' },
		{ n: 1, title: '分享记录', url: '/static/logo.png' },
		{ n: 1, title: '设置', url: '/static/logo.png' }])
</script>

<style lang="scss">
	.navigation {
		flex: 1;
		display: flex;
		flex-direction: column;
		align-items: center;
		width: 100%;
		background-color: #0c121e;

		.topView {
			display: flex;
			flex-direction: row;
			width: 80%;
			margin-top: 20rpx;
		}

		.userMessage {
			display: flex;
			flex-direction: row;
			width: 80%;
			height: 200rpx;
			// background-color: red;
			border-radius: 50rpx;
			margin-top: 20rpx;

			.userImageBox {
				margin: 4%;
				border-radius: 50%;
				width: 144rpx;
				height: 144rpx;

				.userImage {}
			}

			.userName {
				// background-color: aliceblue;
				width: 100%;
				display: flex;
				flex-direction: column;
				padding: 4%;
				justify-content: start;

			}
		}

		.userMessageList {
			width: 80%;
			background: linear-gradient(0deg, #202d42 0%, #0d121b 100%);
			height: 200rpx;
			border-radius: 50rpx;
			display: flex;
			flex-direction: column;
			justify-content: center;

			.userMessageNumber {
				display: flex;
				flex-direction: row;
				justify-content: space-around;
			}

			.userMessageNumberName {
				display: flex;
				flex-direction: row;
				justify-content: space-around;
				margin-top: 20rpx;
			}
		}

		.toolsList {
			width: 80%;
			background-color: #17202f;
			height: 400rpx;
			border-radius: 30rpx;
			margin-top: 20rpx;
			display: flex;
			flex-direction: column;
			justify-content: space-around;
			align-items: start;

			.list {
				width: 90%;
				display: flex;
				flex-direction: row;
				align-items: center;
				margin-left: 20rpx;
			}
		}
	}
</style>